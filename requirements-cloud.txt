# Cloud-sweep pinned dependencies for the Provision-Aware paper.
#
# Use on the cloud machine:
#     python3 -m venv venv && source venv/bin/activate
#     pip install -r requirements-cloud.txt
#
# Versions are pinned to whatever the laptop venv was running on 2026-04-29
# so the cloud reproduces local setup-time behavior exactly. Bump
# deliberately, not opportunistically.

# --- core scientific stack -------------------------------------------------
numpy==2.3.5
pandas==2.3.3
scipy==1.16.3
networkx==3.6.1
matplotlib==3.10.8
joblib==1.5.2

# --- forecasting models ----------------------------------------------------
statsmodels==0.14.6     # SARIMAX, Holt-Winters, seasonal-naive
torch==2.9.1            # LSTM, PatchTST, iTransformer, DLinear
scikit-learn==1.8.0     # metrics, splits

# --- I/O for real datasets -------------------------------------------------
pyarrow==24.0.0         # CESNET-TimeSeries24 parquet reader

# --- conformal prediction (week 5–6) ---------------------------------------
# mapie wraps split CP and CQR; pin >=0.9 for the regression-based wrappers
# we'll use in src/calibration/cqr.py.
mapie>=0.9.0,<1.0

# --- testing ---------------------------------------------------------------
pytest==8.4.2

# --- foundation-model baselines --------------------------------------------
# Chronos-Bolt zero-shot. Pulls transformers + accelerate transitively;
# weights download on first call (~50 MB for tiny, ~3 GB for large).
chronos-forecasting>=1.4
transformers>=4.40
accelerate>=0.30

# --- post-v0 baselines (uncomment when wiring up) --------------------------
# uni2ts>=1.2     # Moirai
# darts>=0.32     # N-BEATS, TFT, alternative DLinear
